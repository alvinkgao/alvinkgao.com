import React from "react";

import { Typography } from "@material-ui/core/";

const Blog = () => (
  <div className="App">
    <header className="App-header">
      <Typography variant="h1">Blog Under Construction</Typography>
    </header>
  </div>
);

export default Blog;
